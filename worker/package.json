{
  "name": "worker",
  "version": "1.0.0",
  "description": "A tiny Cloudflare Worker + KV store that lets a signed-in member's playlist follow them across devices. Cost is $0 within Cloudflare's free tier (100k reads / 1k writes per day).",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
